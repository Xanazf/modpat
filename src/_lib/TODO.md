# TODO

1. Functions only (no classes, with exceptions);
2. Make objects/arrays where needed (e.g. `[g/c]pu_math`)

Extract primitives out of the existing code:

* [ ] SOA (can be a class);
* [ ] Checksums (CRC-32);
* [ ] Math (`gpu_`, `cpu_`);
* [ ] Geometry (topology, positioning, etc.);
* [ ] Physics (e.g. particle life);
